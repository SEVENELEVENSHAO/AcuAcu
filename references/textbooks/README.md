# Textbook References

Place original textbook PDFs here without renaming them. Subfolders may be used when helpful.

Run `npm run audit:sync` after adding or removing files so the audit ledger records the current inventory.
